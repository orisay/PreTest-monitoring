spring:
  jackson:
    time-zone: Asia/Seoul
  profiles:
    active: dev, dev-secret

server:
  port: 8080

app:
  datetime-format: "yyyy-MM-dd'T'HH:mm:ss.SSSXXX"


logging:
  level:
    root: WARN
    com.tera.pretest: WARN

